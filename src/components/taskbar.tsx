// This component is no longer used in the macOS theme.
// It is kept in the project to avoid breaking changes, but can be safely removed.
const Taskbar = () => {
    return null;
}

export default Taskbar;
